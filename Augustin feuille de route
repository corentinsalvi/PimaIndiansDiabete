1️⃣ Baseline simple

On commence par 

Logistic Regression
from sklearn.linear_model import LogisticRegression

VA permettre de calculer les probas que les patients soit diabétiques sachant qu'ils sont filles/garcons/vieux/jeune...

AVANTAGES :

-Interprétable
-Stable
- Référence médicale standard

2️⃣ Random Forest / XGBoost

Ensuite :

from sklearn.ensemble import RandomForestClassifier
Souvent beaucoup plus performant.

3️⃣ Réseau de neurones (Deep Learning)

Seulement après.
On a 700k lignes 

Exemple :

from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense

model = Sequential()
model.add(Dense(64, activation='relu', input_shape=(X.shape[1],)))
model.add(Dense(32, activation='relu'))
model.add(Dense(1, activation='sigmoid'))


Plan de projet structuré :

Phase 1 — EDA

Distribution des variables
Corrélations
Boxplots diabétiques vs non
Détection outliers

Phase 2 — Préprocessing

One-hot encoding pour :
gender
ethnicity
smoking_status
df = pd.get_dummies(df, drop_first=True)
Standardisation
Train / validation split

Phase 3 — Modèles

Logistic Regression
Random Forest
XGBoost
Neural Network
Comparer :
Accuracy/Precision/Recall/F1-score/ROC-AUC

Phase 4 — Feature importance

Très important pour la crédibilité médicale :

model.feature_importances_

On doit être capable de dire :

“Le BMI, l’hypertension et les triglycérides sont les variables les plus prédictives.”


